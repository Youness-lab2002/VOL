spring:
  application:
    name: bookingFlight
  jpa:
    database: POSTGRESQL
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  datasource:
    url: jdbc:postgresql://localhost:5432/vol
    username: postgres
    password: postgres
  sql:
    init:
      platform: postgres
      mode: always